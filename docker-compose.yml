services:
  web:
    image: nginx:alpine
    ports:
      - "127.0.0.1:8080:80"
    volumes:
      - ./index.html:/usr/share/nginx/html/index.html:ro
      - ./config.local.js:/usr/share/nginx/html/config.local.js:ro
    restart: unless-stopped
